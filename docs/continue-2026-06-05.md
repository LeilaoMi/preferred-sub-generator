# preferred-sub-generator 续接记录

生成时间：2026-06-05（Asia/Shanghai）
项目路径：`/home/workspace/Projects/preferred-sub-generator`
当前任务：根据 `docs/audit-2026-06-04.md` 分阶段改造项目。

## 当前状态

用户要求：先制定阶段计划，然后按阶段推进；用户连续要求“继续推进”。线上部署/push 前需要单独确认。

当前已完成：阶段 1/P0、阶段 2/P1 小步安全硬化、阶段 3/P2+P3 可靠性与 CI/CD、阶段 4/P2 数据质量与源健康。

启动时仓库已有未提交改动：

```text
 M README.md
 M docs/checklists/cicd.md
?? docs/audit-2026-06-04.md
```

## 已完成改造

### 阶段计划

新增：

- `docs/plan-2026-06-05-hardening.md`

### 阶段 1：P0 隐私边界

修改：

- `src/security/auth.js`
- `src/api/sub.js`
- `src/api/best.js`
- `src/utils/response.js`
- `public/index.html`
- `public/admin.html`
- `public/admin/index.html`
- `tests/auth.test.js`
- `tests/api.test.js`
- `tests/advanced-features.test.js`
- `tests/smoke.test.js`
- `tests/static.test.js`
- `README.md`
- `docs/cloudflare-setup.md`
- `docs/deploy-checklist.md`
- `docs/checklists/security.md`

内容：

- `/sub` 默认需要只读 token。
- `/best` 默认需要只读 token。
- `SUB_PUBLIC=1` 才恢复公开访问。
- `SUB_READ_TOKEN` / `SUB_READ_TOKEN_NEXT` 支持只读 token；未设置时回退 `SUB_TOKEN`。
- `/sub` 用 `private, no-store`，并设置 `Vary: Authorization`。
- `/api/template?token=` 默认拒绝；`ALLOW_QUERY_TOKEN=1` 才兼容。
- 页面只保留 `#token=`，读取后 `history.replaceState` 清理地址栏。

### 阶段 2：P1 安全硬化小步

修改/新增：

- `src/utils/response.js`
- `src/api/health.js`
- `src/security/rate-limit.js`
- `functions/health/full.js`
- `public/robots.txt`
- `tests/health.test.js`
- `tests/rate-limit.test.js`
- `tests/api.test.js`
- `README.md`
- `docs/cloudflare-setup.md`
- `docs/deploy-checklist.md`

内容：

- API 响应统一安全头：`X-Content-Type-Options`、`Referrer-Policy`、`X-Robots-Tag`、`Permissions-Policy`、`Content-Security-Policy`。
- `/health` 公开最小化，不暴露 `templateExists` / `bestIpsCount`。
- 新增 `/health/full`，需要管理 token。
- rate-limit Map 增加过期清理与 5000 上限。
- 新增 robots 禁止索引。

### 阶段 3：P2/P3 可靠性与 CI/CD

修改：

- `scripts/update-kv.js`
- `scripts/lib/kv.js`
- `.github/workflows/update.yml`
- `scripts/preflight.js`
- `tests/update.test.js`
- `tests/preflight.test.js`
- `README.md`
- `docs/checklists/cicd.md`
- `docs/plan-2026-06-05-hardening.md`

内容：

- 新增 `BEST_IPS_VERSION_INDEX`。
- 默认只保留最近 30 个 `BEST_IPS_*` 快照。
- 删除超出保留上限的旧版本 key。
- `STATUS` 增加 `consecutiveFallbacks`、`lastSuccessfulRefreshAt`、`lastRawAvailable`、`averageLatencyNewScan`。
- webhook 通知失败降级为 warning。
- workflow 增加 `permissions: contents: read`、`concurrency`、`timeout-minutes: 20`。
- preflight 增加 workflow 硬化检查。

### 阶段 4：P2 数据质量与源健康

修改：

- `scripts/lib/candidates.js`
- `scripts/lib/check.js`
- `scripts/update-kv.js`
- `tests/update.test.js`
- `README.md`
- `docs/plan-2026-06-05-hardening.md`

内容：

- 远程源读取大小限制：默认 `SOURCE_MAX_BYTES=5242880`，单源可设置 `maxBytes`。
- 新增 `collectCandidatesWithHealth()`。
- 写入 `SOURCE_HEALTH` 到 KV，包含手动源和远程源的 `ok/status/candidates/error/ms`。
- 检测策略开关：`REQUIRE_CF_RAY=1` 默认只保留带 `cf-ray` 的 Cloudflare Edge 验证结果；`ALLOW_TCP_ONLY=1` 可临时兼容 TCP-only。
- 新增测试覆盖超大源拒绝、source health、严格 CF Ray 策略。

## 验证结果

最新验证通过：

```text
npm test
# 73 passed

npm run preflight
# 部署前检查通过

npm run validate:sources
# 手动源校验通过
```

## 尚未执行

- 未部署 Cloudflare Pages。
- 未 push GitHub。
- 未触发 GitHub Actions。

## 下一步建议

继续阶段 5：P4/API 客户端体验 + P5 测试补充的小步项目：

1. `/sub?type=singbox` 设置 `application/json; charset=utf-8`。
2. `/sub` 增加 `Content-Disposition` 文件名提示。
3. 错误响应增加机器可读 `code`（先做 API 层常见错误，不全项目大重构）。
4. `/status` 增加 stale 判定。
5. XSS/HTML 转义测试和前端小步修复。

## 注意事项

- 不要读取 `.secrets/`。
- 线上部署/push 前先向用户确认。
- 当前有大量未提交改动，下一步如要继续仍需小步验证，避免一次性混入 UI 大改。


## 阶段 5 完成状态

已完成 P4/P5 API/客户端体验补强与阶段 4 文档/实现一致性修复：

- `src/api/sub.js`：订阅响应增加 `Content-Disposition` 文件名提示；Sing-box 返回 `application/json`; 空节点和不支持类型返回机器可读 `code`。
- `src/api/status.js`：增加 `stale` / `unhealthy` / `ageSeconds` 判定。
- `scripts/update-kv.js`：`SOURCE_MAX_BYTES`、`VERSION_RETENTION` 已真正接入更新逻辑；`fetchImpl` 可注入，便于测试。
- `scripts/lib/candidates.js`：全局远程源最大读取字节限制生效。
- `tests/api.test.js`、`tests/smoke.test.js`、`tests/update.test.js`：补齐阶段 5 回归测试。

验证结果：

```text
node --test tests/api.test.js tests/smoke.test.js tests/update.test.js  # 38 passed
npm test                                                              # 76 passed
npm run preflight                                                     # 部署前检查通过
npm run validate:sources                                              # 手动源校验通过
git diff --check                                                      # 通过
```

当前状态：未部署、未 push。

下一步建议：继续处理 public 页面 `innerHTML` 的 XSS 转义/DOM 化，之后再做 `/versions` API 或部署前最终审查。

## 2026-06-05 阶段 6：前端 innerHTML/XSS 收敛

### 已修改文件
- `public/index.html`: 新增 `escapeHtml()`；订阅列表、模板预览、错误消息中的动态值统一转义。
- `public/admin.html`: 新增 `escapeHtml()`；模板预览字段 key/value 转义。
- `public/admin/index.html`: 同步 admin 预览转义。
- `tests/static.test.js`: 增加首页/admin/nested admin 转义断言。

### 验证结果
- `node --test tests/static.test.js`: 5 passed。
- `npm test`: 77 passed。
- `npm run preflight`: 部署前检查通过。
- `npm run validate:sources`: 手动源校验通过。
- `git diff --check`: 通过。

### 状态
- 未部署，未 push。

## 2026-06-05 阶段 7：新增 `/versions` API

### 已修改/新增文件
- `src/api/versions.js`: 新增只读鉴权的版本索引 API。
- `functions/versions.js`: Pages Functions 入口。
- `src/api/kv.js`: 新增 `readBestIpVersions()`。
- `tests/api.test.js`: 增加 `/versions` 鉴权和 `?n=` 限制测试。
- `README.md`、`docs/cloudflare-setup.md`、`docs/deploy-checklist.md`: 同步接口说明和上线验证项。

### 行为
- `GET /versions` 需要只读 token。
- 读取 `BEST_IPS_VERSION_INDEX`。
- 只返回版本索引，不直接返回节点详情。

### 验证
- `node --test tests/api.test.js`: 20 passed。
- `npm test`: 79 passed。
- `npm run preflight`: 部署前检查通过。
- `npm run validate:sources`: 手动源校验通过。
- `git diff --check`: 通过。

### 状态
- 未部署，未 push。

## 2026-06-05 阶段 8：订阅接口兼容性

### 已修改文件
- `src/api/sub.js`: `/sub` 支持 `slot=1` 作为 `template=1` 别名；`type=v2rayng/base64` 支持 `wrap=76` 固定宽度换行。
- `tests/api.test.js`: 增加 `slot` 别名与 base64 wrap 测试。
- `README.md`: 同步 `slot` / `wrap` 参数说明。

### 验证
- `node --test tests/api.test.js`: 22 passed。

### 状态
- 未部署，未 push。
# 续接记录：preferred-sub-generator 硬化推进

## 当前最终状态
- 本地功能硬化和文档同步已推进到可提交前状态。
- 未 push GitHub。
- 未部署 Cloudflare Pages。
- 线上操作前仍需用户单独确认。

## 本轮新增收尾
- 已检查 `docs/checklists/documentation.md`，把文档补全清单全部同步为已完成。
- 修正旧表述：当前 `/sub`、`/best` 默认需要 `SUB_READ_TOKEN`，不是无 token；禁止放进订阅 URL 的是管理 token `SUB_TOKEN`。

## 验证结果
- `npm test`：81 passed，0 failed。
- `npm run preflight`：部署前检查通过。
- `npm run validate:sources`：手动源校验通过。
- `git diff --check`：无输出，无空白错误。
- 敏感值扫描：只命中测试假 token `secret-token` 和审计文档占位说明，没有真实密钥。

## 下一步
1. 逐文件总 diff 审查，确认没有不相关改动。
2. 需要用户确认后再执行 git commit / push。
3. 需要用户确认后再执行 Cloudflare Pages 部署和线上接口验证。
