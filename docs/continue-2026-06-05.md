# preferred-sub-generator 续接记录（2026-06-05）

> 最新状态补充：本文记录的是早期部署 `96b2ddb9` 的历史状态。当前最新已验证部署见 `docs/continue-2026-06-05-read-token-deploy.md`，生产自定义域名仍为 `https://yxdy.woniu.bee.al`，最新预览为 `https://b2f90a26.preferred-sub-generator-zrd.pages.dev`，并已验证 `/api/read-token` 会让首页订阅链接自动附带 `SUB_READ_TOKEN`。


## 任务目标
按用户确认，将项目部署到 Cloudflare Pages，并验证线上可用性。部署前已完成安全加固、Secret 名称同步、测试和 push。

## 项目路径
`/home/workspace/Projects/preferred-sub-generator`

## 当前 Git 状态
- 分支：`main`
- 最新 commit：`1d1448e Harden subscription service before deployment`
- 状态：本地 `main` 与 `origin/main` 对齐（部署前检查为 `main...origin/main`）

## 已完成事项
1. GitHub Secret 名称同步为 `CLOUDFLARE_API_TOKEN_2`：
   - `.github/workflows/update.yml`：从 `secrets.CLOUDFLARE_API_TOKEN_2` 注入运行时 `CLOUDFLARE_API_TOKEN`
   - `scripts/preflight.js`、测试、README 和 docs 说明同步
2. 部署前验证：
   - `npm test`：81 passed / 0 failed
   - `npm run preflight`：通过
   - `npm run validate:sources`：通过
   - `git diff --check`：通过
3. Git 操作：
   - `git add .`
   - `git commit -m "Harden subscription service before deployment"`
   - `git push origin main`
4. Cloudflare Pages 手动部署：
   - 使用 `CLOUDFLARE_API_TOKEN_2` 临时映射为 Wrangler 所需的 `CLOUDFLARE_API_TOKEN`
   - 命令：`CLOUDFLARE_API_TOKEN="$CLOUDFLARE_API_TOKEN_2" npx wrangler pages deploy public --project-name=preferred-sub-generator --commit-dirty=true`
   - 部署成功预览地址：`https://96b2ddb9.preferred-sub-generator-zrd.pages.dev`

## 线上验证结果
验证域名：
- `https://96b2ddb9.preferred-sub-generator-zrd.pages.dev`
- `https://preferred-sub-generator-zrd.pages.dev`
- `https://yxdy.woniu.bee.al`

验证结果：
- `/`：HTTP 200，首页 HTML 正常，`lang="zh-CN"`
- `/health`：HTTP 200，返回 `{ "ok": true }`
- `/status`：HTTP 200，返回 `status: "ok"`、`available: 50`、`checked: 525`、`fallbackActive: false`、`lastError: null`
- `/sub?type=v2rayng` 未带只读 token：HTTP 401，返回 `{ "error": "Unauthorized" }`，符合私密模式预期

## 关键原始输出
Wrangler 部署：
```text
✨ Compiled Worker successfully
✨ Success! Uploaded 4 files (1.43 sec)
✨ Uploading Functions bundle
🌎 Deploying...
✨ Deployment complete! Take a peek over at https://96b2ddb9.preferred-sub-generator-zrd.pages.dev
```

`/status` 样例：
```json
{
  "updatedAt": "2026-06-04T19:54:52.110Z",
  "available": 50,
  "newAvailable": 50,
  "sourceCount": 6,
  "checked": 525,
  "fallbackActive": false,
  "lastError": null,
  "stale": false,
  "unhealthy": false,
  "ageSeconds": 10014,
  "status": "ok"
}
```

## 未完成/需注意
- 未验证带 `SUB_READ_TOKEN` 的 `/sub`、`/best` 内容，因为当前会话环境没有暴露 `SUB_READ_TOKEN`/`SUB_TOKEN` 值；不能输出或猜测密钥。
- 如果用户提供或已在环境中配置只读 token，可继续用 `Authorization: Bearer <SUB_READ_TOKEN>` 或短参数 `t=` 验证订阅内容。
- 本文档不包含任何密钥、token 或 cookie。

## 最终状态
已部署、已 push、公开健康检查和状态接口验证通过；私密订阅接口未授权访问返回 401，符合安全预期。
